@model List<GCTL.Core.ViewModels.SupplierCategory.SupplierCategorySetupViewModel>

<table id="SupplierCategory-grid" class="table table-striped  table-bordered table-sm table-hover table-common">
    <thead class="row-border">
        <tr class="table row-border">
            <th class="text-center text-middle" style="width: 10%;">
                Select <br /> <input type="checkbox" id="SupplierCategory-check-all" />
            </th>
            <th class="text-center text-middle" style="width: 10%;">ID</th>
            <th class="text-center text-middle" style="width: 80%;"> Color</th>

        </tr>
    </thead>
    <tbody>

        @if (Model == null || !Model.Any())
        {
            <tr class="text-center text-middle">
                <td colspan="8">No Data Available.</td>
            </tr>
        }
        else
        {
            @foreach (var item in Model.OrderByDescending(x => x.SupplierCategoryId))
            {
                <tr>
                    <td class="text-center">
                        <input type="checkbox" class="text-center text-middle checkBox" data-id="@item.SupplierCategoryId" value="@item.SupplierCategoryId" />
                    </td>
                    <td>
                        <a href="#" class='btn js-SupplierCategory-edit'
                           style="text-decoration: underline; color: rebeccapurple;"
                           data-id="@item.SupplierCategoryId" title="Edit">
                            @item.SupplierCategoryId
                        </a>
                    </td>

                    <td class="text-center text-middle">@item.SupplierCategory</td>

                </tr>
            }
        }

    </tbody>
</table>