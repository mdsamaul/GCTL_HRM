@model List<GCTL.Core.ViewModels.SupplierType.SupplierTypeSetupViewModel>

<table id="SupplierType-grid" class="table table-striped  table-bordered table-sm table-hover table-common">
    <thead class="row-border">
        <tr class="table row-border">
            <th class="text-center text-middle" style="width: 10%;">
                Select <br /> <input type="checkbox" id="SupplierType-check-all" />
            </th>
            <th class="text-center text-middle" style="width: 10%;">ID</th>
            <th class="text-center text-middle" style="width: 80%;"> Supplier Type Name</th>

        </tr>
    </thead>
    <tbody>

        @if (Model == null || !Model.Any())
        {
            <tr class="text-center text-middle">
                <td colspan="8">No Data Available.</td>
            </tr>
        }
        else
        {
            @foreach (var item in Model.OrderByDescending(x => x.SupplierTypeId))
            {
                <tr>
                    <td class="text-center">
                        <input type="checkbox" class="text-center text-middle checkBox" data-id="@item.SupplierTypeId" value="@item.SupplierTypeId" />
                    </td>
                    <td>
                        <a href="#" class='btn js-SupplierType-edit'
                           style="text-decoration: underline; color: rebeccapurple;"
                           data-id="@item.SupplierTypeId" title="Edit">
                            @item.SupplierTypeId
                        </a>
                    </td>

                    <td class="text-center text-middle">@item.SupplierTypeName</td>

                </tr>
            }
        }

    </tbody>
</table>