@model GCTL.UI.Core.ViewModels.FebricTest.FebricTestPageViewModel

@{
    Model.PageTitle = ViewBag.Title;
    ViewBag.Title = "Febric Test";
    Model.Breadcrumb = "Febric Test";
}

<!-- Content Header (Page header) -->
<partial name="_PageHeader" model="Model" />

<section class="content consistent-look">
    <div class="container-fluid">
        <div class="row">

            <div class="col-md-6 pr-0">
                <div class="card">
                    <div class="card-header px-2">
                        <partial name="_Toolbars" model="new ToobarModel() { Title = Model.Breadcrumb, Code = Model.Code, Name = Model.Setup.FebricTestD }" />
                    </div>
                    <div class="card-body px-2">

                        <form asp-action="Setup" method="post" id="FebricTest-form" asp-controller="FebricTest">

                            <div class="js-FebricTest-form-container">
                                <partial name="_Setup" model="Model.Setup" />
                            </div>

                        </form>
                    </div>
                </div>
            </div>

            <div class="col-md-6" style="height:570px;overflow:auto;">
                <div class="card">
                    <div class="card-body p-2 pt-3">
                        <div class="js-FebricTest-grid-container">
                            @await Html.PartialAsync("_Grid", Model.FebricTestList)
                        </div>
                    </div>
                </div>
            </div>


        </div>


        <partial name="_BottomNavigation" />
    </div>
</section>
<partial name="DeleteModal" />

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="~/js/pages/febricTest.js"></script>
    <script type="text/javascript">
        var options = {
            baseUrl: '@Url.Action("", "FebricTest")'
        };
        options.load = () => {
        };

        $.febricTest(options);
    </script>
}