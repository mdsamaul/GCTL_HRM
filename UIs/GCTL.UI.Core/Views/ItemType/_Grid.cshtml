@model List<GCTL.Core.ViewModels.ItemType.ItemTypeSetupViewModel>

<table id="ItemType-grid" class="table table-striped  table-bordered table-sm table-hover table-common">
    <thead class="row-border">
        <tr class="table row-border">
            <th class="text-center text-middle" style="width: 10%;">
                Select <br /> <input type="checkbox" id="ItemType-check-all" />
            </th>
            <th class="text-center text-middle" style="width: 10%;">ID</th>
            <th class="text-center text-middle" style="width: 80%;"> Item Name</th>

        </tr>
    </thead>
    <tbody>

        @if (Model == null || !Model.Any())
        {
            <tr class="text-center text-middle">
                <td colspan="8">No Data Available.</td>
            </tr>
        }
        else
        {
            @foreach (var item in Model.OrderByDescending(x => x.ItemTypeId))
            {
                <tr>
                    <td class="text-center">
                        <input type="checkbox" class="text-center text-middle checkBox" data-id="@item.ItemTypeId" value="@item.ItemTypeId" />
                    </td>
                    <td>
                        <a href="#" class='btn js-ItemType-edit'
                           style="text-decoration: underline; color: rebeccapurple;"
                           data-id="@item.ItemTypeId" title="Edit">
                            @item.ItemTypeId
                        </a>
                    </td>

                    <td class="text-center text-middle">@item.ItemName</td>

                </tr>
            }
        }

    </tbody>
</table>