@model List<GCTL.Core.ViewModels.PackageType.PackageTypeSetupViewModel>

<table id="PackageType-grid" class="table table-striped  table-bordered table-sm table-hover table-common">
    <thead class="row-border">
        <tr class="table row-border">
            <th class="text-center text-middle" style="width: 10%;">
                Select <br /> <input type="checkbox" id="PackageType-check-all" />
            </th>
            <th class="text-center text-middle" style="width: 10%;">ID</th>
            <th class="text-center text-middle" style="width: 80%;"> Package Type</th>

        </tr>
    </thead>
    <tbody>

        @if (Model == null || !Model.Any())
        {
            <tr class="text-center text-middle">
                <td colspan="8">No Data Available.</td>
            </tr>
        }
        else
        {
            @foreach (var item in Model.OrderByDescending(x => x.PackageTypeId))
            {
                <tr>
                    <td class="text-center">
                        <input type="checkbox" class="text-center text-middle checkBox" data-id="@item.PackageTypeId" value="@item.PackageTypeId" />
                    </td>
                    <td>
                        <a href="#" class='btn js-PackageType-edit'
                           style="text-decoration: underline; color: rebeccapurple;"
                           data-id="@item.PackageTypeId" title="Edit">
                            @item.PackageTypeId
                        </a>
                    </td>

                    <td class="text-center text-middle">@item.PackageType</td>

                </tr>
            }
        }

    </tbody>
</table>