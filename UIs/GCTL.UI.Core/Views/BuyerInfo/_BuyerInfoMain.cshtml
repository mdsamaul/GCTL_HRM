@* @model BuyerViewModel
@{
    Layout = null; // Modal load করার জন্য খুব গুরুত্বপূর্ণ
}

<div class="container-fluid">
    <div class="card">
        <div class="card-header bg-light">
            <h5 class="card-title">Buyer Information</h5>
            <button type="button" class="btn btn-sm btn-danger float-end js-modal-dismiss">
                <i class="fa fa-times"></i> Close
            </button>
        </div>

        <div class="card-body">
            <nav>
                <div class="nav nav-tabs m-3 mb-0" id="nav-tab" role="tablist">
                    <button class="nav-link active" id="nav-home-tab" data-bs-toggle="tab" data-bs-target="#nav-home" type="button">Buyer Info</button>
                    <button class="nav-link" id="nav-profile-tab" data-bs-toggle="tab" data-bs-target="#nav-profile" type="button">Brand Info</button>
                    <button class="nav-link" id="nav-contact-tab" data-bs-toggle="tab" data-bs-target="#nav-contact" type="button">Delivery Address</button>
                </div>
            </nav>

            <div class="tab-content p-3" id="nav-tabContent">
                <div class="tab-pane fade show active" id="nav-home" role="tabpanel">
                    <partial name="_BuyerInfo" model="Model" />
                </div>
                <div class="tab-pane fade" id="nav-profile" role="tabpanel">
                    <partial name="_BrandInfo" model="Model" />
                </div>
                <div class="tab-pane fade" id="nav-contact" role="tabpanel">
                    <partial name="_DLAddress" model="Model" />
                </div>
            </div>
        </div>
    </div>
</div>

<input type="hidden" id="lastCode" value="@Model.Code" />
 *@