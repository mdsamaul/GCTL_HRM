@model List<GCTL.Core.ViewModels.SeasonInformation.SeasonInformationSetupViewModel>

<table id="SeasonInformation-grid" class="table table-striped  table-bordered table-sm table-hover table-common">
    <thead class="row-border">
        <tr class="table row-border">
            <th class="text-center text-middle" style="width: 10%;">
                Select <br /> <input type="checkbox" id="SeasonInformation-check-all" />
            </th>
            <th class="text-center text-middle" style="width: 10%;">ID</th>
            <th class="text-center text-middle" style="width: 40%;"> Season</th>
            <th class="text-center text-middle" style="width: 40%;">Detail</th>

        </tr>
    </thead>
    <tbody>

        @if (Model == null || !Model.Any())
        {
            <tr class="text-center text-middle">
                <td colspan="8">No Data Available.</td>
            </tr>
        }
        else
        {
            @foreach (var item in Model.OrderByDescending(x => x.SeasonId))
            {
                <tr>
                    <td class="text-center">
                        <input type="checkbox" class="text-center text-middle checkBox" data-id="@item.SeasonId" value="@item.SeasonId" />
                    </td>
                    <td>
                        <a href="#" class='btn js-SeasonInformation-edit'
                           style="text-decoration: underline; color: rebeccapurple;"
                           data-id="@item.SeasonId" title="Edit">
                            @item.SeasonId
                        </a>
                    </td>

                    <td class="text-center text-middle">@item.Season</td>
                    <td class="text-center text-middle">@item.Detail</td>

                </tr>
            }
        }

    </tbody>
</table>