@model GCTL.UI.Core.ViewModels.Doctors.DoctorPageViewModel
@{
    ViewBag.Title = "Doctor List";
    Model.PageTitle = ViewBag.Title;
    Model.Breadcrumb = "Doctor Info";
}

<!-- Content Header (Page header) -->
<section class="content-header" style="padding-bottom: 0;">
    <div class="container-fluid">
        <div class="calloutx callout-info" style="height: 35px;">
            <div class="row mb-2">
                <div class="col-md-8 row">
                    <ol class="breadcrumb float-sm-right">
                        <li class=""><a href="@Url.Action("Setup", "Doctors")" class="linkbutton1 btn btn-sm btn-success" style="font-size:13px; text-decoration: none;"> <i class="fa fa-plus"></i> Add Doctor </a></li>
                    </ol>
                </div>
                <div class="col-md-4">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="/Dashboard">Home</a></li>
                        <li class="breadcrumb-item active">Doctor Info</li>
                    </ol>
                </div>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header border-transparent">
                        <h3 class="card-title">@ViewBag.Title</h3>
                    </div>
                    <!-- /.card-header -->
                    <div class="card-body p-2 pt-3">
                       <partial name="_Filter" model="Model.Filter" />
                        <div class="js-doctor-grid-container">
                            <partial name="_Grid" />
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <partial name="_BottomNavigation" />
    </div>
</section>
<partial name="DeleteModal" />

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script src="~/js/pages/doctors.js?v=1.0.1"></script>
    <script type="text/javascript">
        var options = {
            baseUrl: '@Url.Action("", "Doctors")'
        };
        options.load = () => {
        };

        $.doctors(options);
    </script>
}



