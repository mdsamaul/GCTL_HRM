@model List<GCTL.Core.ViewModels.SizeInformation.SizeInformationSetupViewModel>

<table id="SizeInformation-grid" class="table table-striped  table-bordered table-sm table-hover table-common">
    <thead class="row-border">
        <tr class="table row-border">
            <th class="text-center text-middle" style="width: 10%;">
                Select <br /> <input type="checkbox" id="SizeInformation-check-all" />
            </th>
            <th class="text-center text-middle" style="width: 10%;">ID</th>
            <th class="text-center text-middle" style="width: 40%;"> Size</th>
            <th class="text-center text-middle" style="width: 40%;">Detail</th>

        </tr>
    </thead>
    <tbody>

        @if (Model == null || !Model.Any())
        {
            <tr class="text-center text-middle">
                <td colspan="8">No Data Available.</td>
            </tr>
        }
        else
        {
            @foreach (var item in Model.OrderByDescending(x => x.SizeId))
            {
                <tr>
                    <td class="text-center">
                        <input type="checkbox" class="text-center text-middle checkBox" data-id="@item.SizeId" value="@item.SizeId" />
                    </td>
                    <td>
                        <a href="#" class='btn js-SizeInformation-edit'
                           style="text-decoration: underline; color: rebeccapurple;"
                           data-id="@item.SizeId" title="Edit">
                            @item.SizeId
                        </a>
                    </td>

                    <td class="text-center text-middle">@item.Size</td>
                    <td class="text-center text-middle">@item.Detail</td>

                </tr>
            }
        }

    </tbody>
</table>