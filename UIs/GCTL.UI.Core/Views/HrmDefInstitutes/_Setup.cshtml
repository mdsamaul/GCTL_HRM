@model GCTL.Core.ViewModels.HrmDefInstitutes.HrmDefInstitutesSetupViewModel

<div style="padding-top: 10px;">
    <div class="row">
        <div class="col-lg-6">
            <span id="message"></span>
        </div>
        <div class="col-lg-6">
        </div>
    </div>
    <div class="form-group row">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <input type="hidden" id="lastCode" />
        <input type="hidden" id="AutoId" asp-for="AutoId" />
        <input type="hidden" asp-for="Id" />
        <input type="hidden" asp-for="Ldate" />
        <input type="hidden" asp-for="ModifyDate" />
        <div class="col-lg-6">
            <label asp-for="InstituteCode">Institute Id</label>
            <div class="input-group input-group-sm">
                <input asp-for="InstituteCode" type="text" class="form-control form-control-sm js-InstituteCode-code" readonly="readonly" />
            </div>
        </div>
    </div>
    <div class="form-group row">
        <div class="col-lg-6">
            <label asp-for="InstituteName">Institute Name<span style="color:red">*</span> </label>
            <input type="text" asp-for="InstituteName" class="form-control form-control-sm  js-HrmDefInstitutes-check-availability" autofocus required placeholder="Enter  Institute Name" tabindex="1" />
            @* <span asp-validation-for="DegreeName" class="text-danger"></span> *@

        </div>
        <div class="col-lg-6">
            <label asp-for="ShortName">Short Name</label>
            <input type="text" asp-for="ShortName" class="form-control form-control-sm" placeholder="Enter Short Name" tabindex="2" />
        </div>
    </div>






    @if (Model.AutoId > 0)
    {

        <hr />
        <div class="form-group row" id="LdateModifyHide">
            <div class="col-lg-6">
                @if (Model.Ldate.HasValue)
                {
                    <label>Creation Date: </label>

                    <label>@Model.Ldate?.ToString("dd/MM/yyyy")</label>
                }
            </div>
            <div class="col-lg-6">
                @if (Model.ModifyDate.HasValue)
                {
                    <label>Last Update Date: </label>

                    <label>@Model.ModifyDate?.ToString("dd/MM/yyyy")</label>
                }
            </div>
        </div>

    }

</div>
<div class="row no-print" style="margin-top: 9px;">
    <div class="col-12">
        <button type="button" class="btn btn-default success-btn btn btn-outline-success btn-common btn-circle float-right js-HrmDefInstitutes-save" data-id="@Model.InstituteCode">
            <i class="fas fa-save"></i> Save
        </button>
    </div>
</div>




