<style>
    .table thead th {
        background-color: #f8f9fa;
        border-bottom: 2px solid #dee2e6;
        font-weight: 600;
    }

    .btn-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border: none;
    }

    .dataTables_wrapper .dataTables_paginate .paginate_button.current {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
        border-color: #667eea !important;
    }

    .loading-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0,0,0,0.5);
        display: none;
        justify-content: center;
        align-items: center;
        z-index: 9999;
    }

    .spinner {
        width: 50px;
        height: 50px;
        border: 5px solid #f3f3f3;
        border-top: 5px solid #667eea;
        border-radius: 50%;
        animation: spin 1s linear infinite;
    }
    @@keyframes spin {
        0%

    {
        transform: rotate(0deg);
    }

    100% {
        transform: rotate(360deg);
    }

    }

    /* Pagination container */
    .dataTables_paginate {
        display: flex;
        justify-content: end;
        align-items: center;
        margin-top: 20px;
        gap: 5px;
        cursor:pointer;
    }

        /* Page number buttons */
        .dataTables_paginate a.paginate_button {
            background-color: #f0f0f0;
            color: #333;
            padding: 6px 12px;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin: 0 3px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
        }

            /* Hover effect */
            .dataTables_paginate a.paginate_button:hover {
                background-color: #007bff;
                color: white;
                border-color: #007bff;
            }

            /* Active/current page */
            .dataTables_paginate a.paginate_button.current {
                background-color: #007bff;
                color: white;
                border-color: #007bff;
                font-weight: bold;
            }

            /* Disabled buttons */
            .dataTables_paginate a.paginate_button.disabled {
                background-color: #e9ecef;
                color: #6c757d;
                border-color: #dee2e6;
                cursor: not-allowed;
            }

            /* Remove outline on focus */
            .dataTables_paginate a.paginate_button:focus {
                outline: none;
                box-shadow: none;
            }

    #advancePayTable_filter {
        float: right;
        margin-bottom: 10px;
    }

        #advancePayTable_filter label {
            font-weight: bold;
            font-size: 14px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        #advancePayTable_filter input[type="search"] {
            padding: 6px 12px;
            font-size: 14px;
            border: 1px solid #ccc;
            border-radius: 4px;
            outline: none;
            transition: all 0.3s ease;
            width: 15vw;
            
        }

            #advancePayTable_filter input[type="search"]:focus {
                border-color: #0d6efd;
                box-shadow: 0 0 5px rgba(13, 110, 253, 0.5);
            }

    #advancePayTable thead th {
        text-align: center !important;
        vertical-align: middle !important;
    }
   

  

</style>

<!-- DataTable -->
<div class="table-responsive">
    <table id="advancePayTable" class="table table-striped table-hover text-center table-common" style="width:100%">
        <thead>
            <tr>
                <th>Select
                    <input type="checkbox" id="selectAllCheckbox" />
                </th>
                <th>Adjustment ID</th>
                <th>Employee ID</th>
                <th>Name</th>
                <th>Designation</th>
                <th>Loan ID</th>
                <th>No. Of Inst(s)</th>
                <th>Loan Amount</th>
                <th>Monthly Deduction</th>
                <th>Salary Month</th>
                <th>Salary Year</th>   
            </tr>
        </thead>
        <tbody>
            <!-- Data will be loaded here -->
        </tbody>
    </table>
</div>