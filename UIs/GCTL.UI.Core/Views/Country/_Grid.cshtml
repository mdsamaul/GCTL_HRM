@model List<GCTL.Core.ViewModels.Country.CountrySetuoViewModel>

<table id="Country-grid" class="table table-striped  table-bordered table-sm table-hover table-common">
    <thead class="row-border">
        <tr class="table row-border">
            <th class="text-center text-middle" style="width: 10%;">
                Select <br /> <input type="checkbox" id="Country-check-all" />
            </th>
            <th class="text-center text-middle" style="width: 10%;">ID</th>
            <th class="text-center text-middle" style="width: 80%;"> Country Name</th>

        </tr>
    </thead>
    <tbody>

        @if (Model == null || !Model.Any())
        {
            <tr class="text-center text-middle">
                <td colspan="8">No Data Available.</td>
            </tr>
        }
        else
        {
            @foreach (var item in Model.OrderByDescending(x => x.CountryId))
            {
                <tr>
                    <td class="text-center">
                        <input type="checkbox" class="text-center text-middle checkBox" data-id="@item.CountryId" value="@item.CountryId" />
                    </td>
                    <td>
                        <a href="#" class='btn js-Country-edit'
                           style="text-decoration: underline; color: rebeccapurple;"
                           data-id="@item.CountryId" title="Edit">
                            @item.CountryId
                        </a>
                    </td>

                    <td class="text-center text-middle">@item.CountryName</td>

                </tr>
            }
        }

    </tbody>
</table>